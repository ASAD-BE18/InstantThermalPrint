Prereqs:
0.  Use the install script to install and configure the print software (CUPS) 
	and the printer driver (ZJ-58), also get gphoto2.
	
Basic flow:
1.	Run script that queries the camera's SD card for new files (gphoto2 -L)
2.	Fetch the file numbers and add them to whitelist, checking for changes
3.	If the list had changed, pull the changed files from the camera using their number (gphoto2 -P <number_range>)
4.	Send these pictures to the printer to print (lp -o fit-to-page <filenames>)
5.	Remove the pictures locally.